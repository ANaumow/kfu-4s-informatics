package ru.naumow.pages.position;

import ru.naumow.pages.browser.Browser;

public interface Position {

    void gotoAdv(Browser browser);

    void show(Browser browser); // start playing video, or showing text
}
